program example1; (* print number between *)
var a : integer; b: integer;
BEGIN
    a := readi;
    b := readi;

    if a > b Call WriteI(a - b - 1) ;
    else 
        if a = b then Call WriteI(0) else Call WriteI(b - a - 1);
END. 